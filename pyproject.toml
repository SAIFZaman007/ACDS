[project]
name = "air-draw"
version = "0.1.0"
description = "Draw in the air using real-time hand-gesture tracking (OpenCV + MediaPipe)."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "opencv-python>=4.9",
    "mediapipe>=1.0.0",
    "numpy>=1.26",
]

[tool.uv]
package = false